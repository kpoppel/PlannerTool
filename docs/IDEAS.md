# Possible future work/ideas/features

A list of TODOs, ideas for improvement etc., which are/were not captured as Git issues.
Maybe they should, maybe they will never be done.

When something is completed, remove it from the list.

- Feature: Make it possible to edit description in the UI
- Feature: Allow user to configure which projects and teams to load (reducing load time a tiny amount)
- Feature (convenience) Export graph view data to Excel format.

- Feature: Build multi department compartments: Further group teams and departments in teh server config
  and allow users to select whole departments or pick and choose.
  Alternative: just deploy a server per department - this is much easier and more performant anyway. (Current solution)
- Feature: Permission system to enable read/only (viewer) access, disable access to certain tools etc.
