# PlannerTool Docker Environment Variables
# Copy this file to .env and fill in the values

# Required: Encryption key for Personal Access Tokens (PATs)
# Generate with: openssl rand -base64 32
# IMPORTANT: Keep this key secure and backed up! Without it, encrypted PATs cannot be decrypted.
PLANNER_SECRET_KEY=your-secret-key-here
