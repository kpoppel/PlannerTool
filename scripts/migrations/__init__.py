"""Migrations package for PlannerTool.

Place migration files here named with a numeric prefix, e.g.
`0000_initial.py`. Each migration should expose `MIGRATION_ID` and
`upgrade(dry_run=False, backup=False)`.
"""
