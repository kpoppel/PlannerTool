/**
 * DependencyRenderer.lit.js — REMOVED
 *
 * This module has been converted into a proper overlay plugin.
 * Use PluginDependenciesComponent (plugins/PluginDependenciesComponent.js) instead.
 *
 * The plugin auto-activates at startup via modules.config.json (activated: true)
 * and subscribes to all the same events the former initDependencyRenderer() handled.
 *
 * If you need to update tests that imported initDependencyRenderer, change the import to:
 *   import { PluginDependenciesComponent } from '../plugins/PluginDependenciesComponent.js';
 */
